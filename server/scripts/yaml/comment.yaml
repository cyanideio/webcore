---
- config:
    - testset: "Comment Test"
    - timeout: 100  # Increase timeout from the default 10 seconds
- test: 
    - name: "GET COMMENT"
    - url: "/api/v1/comment/"
    - expected_status: [401]